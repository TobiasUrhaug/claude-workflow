# claude-workflow

A structured workflow system for managing software features with Claude. It organizes work into roles (Analyst, Architect, Developer, Reviewer) and tracks features through a defined lifecycle using markdown files stored in `.claude/features/`.